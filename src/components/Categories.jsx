import styles from '../sass/Categories.module.scss';

const Categories = () => (
  <div className={styles.btn_wrapper}>
    <button type="button" className={styles.check_btn}>Check Status</button>
  </div>
);

export default Categories;
